<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Test">
	<select id="select">
		SELECT 
			b_seq,
			display_yn,
			title,
			content,
			attach_path,
			attach_file,
			hit,
			in_date, 
			in_user_id, 
			up_date, 
			up_user_id
 		from 
 			snotice 
 		where 
 			b_seq = :1
	</select>
	<update id="insert">
		insert into snotice 
		(
			b_seq,
			display_yn,
			title,
			content,
			attach_path,
			attach_file,
			hit, 
			in_date, 
			in_user_id, 
			up_date, 
			up_user_id
		) 
		values
		(
			:1, 
			:2, 
			:3, 
			:4, 
			:5,
			:6,
			:7, 
			to_char(sysdate, 'YYYYMMDD.HH24MISS'), 
			:8, 
			to_char(sysdate, 'YYYYMMDD.HH24MISS'), 
			:9
		)
	</update>
	<update id="update">
		update 
			snotice 
		set 
			title			    =:1, 
			content				=:2, 
			attach_path			=:3, 
			attach_file			=:4, 
			hit					=:5, 
			up_date				= to_char(sysdate, 'YYYYMMDD.HH24MISS'), 
			up_user_id			=:6 
		where 
			b_seq=:7 
	</update>
	<update id="delete">
		delete from snotice where b_seq = :7
	</update>
</mapper>